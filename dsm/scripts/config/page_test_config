########## PATHS ##########

# Location of the source benchmark
APP_SOURCE_DIR="$HOME/criu/dsm/test"

# Executable (must exist inside APP_SOURCE_DIR)
APP_EXEC="page_test"

########## RUN PARAMS ##########

# Arguments passed to the binary
APP_ARGS="3 512 100"
# Additional input files required for correct execution (optional)
APP_EXTRA_FILES=()   # histogram-dsm doesn't need extra files

# Warm up time before CRIU dump
APP_WARMUP=2

########## CLIENT MACHINES ##########
CLIENTS=("dsm_client" "dsm_client2" )

# Number of threads for DSM (main thread count)
APP_THREADS=3
CONFIGS=(
    #"base   0     0-1   tcp"
    #"2node 2 0-0 1-1 tcp"
    "split1_1_tcp   2   0-0  1-1  2-2    tcp"
    #"split1_1_rdma   2   0-0  1-1  2-2    rdma"
    #"split4_4_rdma  2   0-0  1-1  2-2      rdma"
    #"split4_2_2_tcp 2 0-0 1-1 2-2 tcp"
    #"split4_2_2_rdma  2  0-3     4-5     6-7     rdma"
)

########## SCRIPT PATHS ##########
SCRIPTS="$HOME/criu/dsm/scripts"
THREAD_FILTER="$SCRIPTS/thread_filter_ranged.py"
